{
  "name": "payment-service",
  "version": "1.0.0",
  "description": "Minimal fake payment authorizer with a CHAOS_MODE toggle. Exists to be a realistic synchronous HTTP host for the Istio service mesh demo, not a production payment system.",
  "main": "build/index.js",
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^24.0.0",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js"
  }
}
